/**
 * you can choose to keep all
 * enums for your app in here or separate them
 * however you choose to.
 */

export enum Cookies {
  ID_TOKEN = 'astra-id-token', // change to preffered name
  APP_PUBLIC_KEY = 'astra-app-public-key', // change to preffered name
  MAX_AGE = 30 * 24 * 60 * 60, // 30 days (1 month)
}
